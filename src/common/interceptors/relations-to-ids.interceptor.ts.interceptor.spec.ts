import { RelationsToIdsInterceptor } from './relations-to-ids.interceptor';

describe('RelationsToIdsInterceptorTsInterceptor', () => {
  it('should be defined', () => {
    expect(new RelationsToIdsInterceptor()).toBeDefined();
  });
});
