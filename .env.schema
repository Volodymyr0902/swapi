APP_PROTOCOL=http
APP_HOST=0.0.0.0
APP_PORT=8080

MYSQL_HOST=mysql (Docker) or localhost (local)
MYSQL_PORT=3306
MYSQL_DATABASE=swapi
MYSQL_USER=rootuser
MYSQL_PASSWORD=mysql00$
MYSQL_ROOT_PASSWORD=mysql00$

AWS_REGION=eu-north-1
AWS_ACCESS_KEY_ID=provide
AWS_SECRET_ACCESS_KEY=provide
AWS_BUCKET=swapi-imgs

AUTH_ACCESS_SECRET=secret-key
AUTH_REFRESH_SECRET=super-secret-key
AUTH_ACCESS_EXP=5m
AUTH_REFRESH_EXP=14d
AUTH_REFRESH_TTL=1209600
AUTH_ONE_TIME_SECRET=one-time-key
AUTH_ONE_TIME_EXP=10m
AUTH_ONE_TIME_TTL=600

REDIS_SESSIONS_URL=redis://localhost:6379/0 (local) redis://redis:6379/0 (Docker)
REDIS_ONE_TIME_URL=redis://localhost:6379/1 (local) redis://redis:6379/1 (Docker)
REDIS_THROTTLER_URL=redis://localhost:6379/2 (local) redis://redis:6379/2 (Docker)

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=schema@mail.com
SMTP_PASSWORD='vlqo zviq xela ernd'
APP_EMAIL_NAME='SWAPI Notifications'

THROTTLE_TTL=60000
THROTTLE_LIMIT=100
